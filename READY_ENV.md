Готовые env-файлы уже лежат в проекте:
- backend/.env
- frontend/.env

Нужно заменить только одно значение:
- GIGACHAT_AUTH_KEY в backend/.env

SECRET_KEY уже сгенерирован и менять его не обязательно для первого запуска.
